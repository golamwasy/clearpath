# ClusterIP only — access via: kubectl port-forward -n clearpath svc/grafana 3000:3000
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: clearpath
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
